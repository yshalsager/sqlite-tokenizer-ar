# Analyzer Fixtures

Input format is TSV:

- column 1: fixture id
- column 2: UTF-8 text encoded as base64

Files:

- `inputs.smoke.tsv`: small deterministic public fixture set for quick checks.

Large corpus analyzer fixtures are intentionally not committed. Keep those in a private validation repository or generate them locally from data you are allowed to use.
