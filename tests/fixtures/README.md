# Fixtures

Versioned fixture inputs/outputs for analyzer and query parity.

Planned layout:

- `analyzer/`: token stream fixtures and expected outputs
- `queries/`: query fixtures and expected ranked hit outputs
- `datasets/`: manifest files for `smoke`, `parity`, and `full` tiers
